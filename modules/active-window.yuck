; (defwidget activewindow []
;   (box :class "active-window"
;     (label :text xtitle :class "wintitle")
;   )
; )
;


; (defwidget activewindow []
;     (box :class "active-window" 
;       (label :text actwindow :class "wintitle")
;     )
; )



(defwidget activewindow []
      (literal :content actwindow :valign "center") 
)
